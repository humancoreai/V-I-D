# --------------------------------------------------
# Voice Intel Desktop – Environment Example
# --------------------------------------------------
# Copy this file to `.env.local` for local development.
# Do NOT commit `.env.local` or any real secrets.
# --------------------------------------------------

# === App ===
NEXT_PUBLIC_APP_NAME=Voice Intel Desktop
NEXT_PUBLIC_APP_ENV=development

# === API / AI Provider (example placeholders) ===
# Leave empty or replace with your own local keys
OPENAI_API_KEY=
OPENAI_MODEL=

# === Optional: Feature Flags ===
FEATURE_VOICE_INPUT=false
FEATURE_DESKTOP_BRIDGE=false
